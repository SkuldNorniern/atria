[package]
name = "atria-compositor"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Backend-independent ATRIA compositor protocol state machine"

[dependencies]
atria-protocol = { path = "../atria-protocol" }

[lints]
workspace = true
