[package]
name = "atria-transport"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Message transport and handle passing for Atria"

[dependencies]
atria-compositor = { path = "../atria-compositor" }
atria-protocol = { path = "../atria-protocol" }
libc = "0.2"

[dev-dependencies]
libc = "0.2"

[lints]
workspace = true
